# ScamStream — Streaming Scam Call Detection

Mô hình phát hiện cuộc gọi lừa đảo theo thời gian thực, sử dụng **HaLong Embedding** + **Causal CrossTurnAttention** + **Focal U-shape Loss** + **Weighted Prefix Auxiliary Loss (Noisy-OR)**.

## Architecture

```
Turn text → HaLong Encoder ([CLS]) → Linear Proj → CrossTurnAttention (causal) → MLP Head → sigmoid → P(scam)
```

- **Encoder**: `contextboxai/halong_embedding` — Vietnamese pretrained embedding, không cần word segmentation
- **CrossTurnAttention**: Causal attention — turn `t` chỉ attend tới các turn trước đó `0..t-1`
- **Loss**: `L_total = L_focal_ushape + λ × L_weighted_prefix`

### 1) Focal × U-shape Loss (Main)

| Cơ chế | Công thức | Vai trò |
|--------|-----------|---------|
| **Focal** | `(1-p_t)^γ` | Focus vào hard examples, down-weight easy |
| **U-shape** | `w(t,N) = (2t/N - 1)² × (1 - w_floor) + w_floor` | Trọng số cao ở đầu/cuối dialogue, thấp ở giữa |
| **Class weight** | `class_weight_harmless = 8.0` | Cân bằng imbalance scam/harmless ở sample level |

### 2) Weighted Prefix Auxiliary Loss (from Streaming-Bert Noisy-OR)

Coi `turn_probs` là evidence probabilities `q_t`, tính xác suất tích lũy bằng **Noisy-OR**:

```
p_t_agg = 1 − ∏_{i=0}^{t} (1 − q_i)    ← cumulative, monotonically non-decreasing
```

Sau đó apply **weighted BCE** trên từng prefix:

```
L_prefix = Σ (2t/N) × BCE(p_t_agg, y)    ← weight tuyến tính tăng (1-based)
```

- **Turn đầu**: phạt nhẹ (model thiếu info)
- **Turn cuối**: phạt nặng (đủ info mà vẫn sai)
- **λ = 0**: tắt auxiliary → trở về pure Focal U-shape
- **λ = 0.5** (default): kết hợp cả hai → thúc đẩy model predict đúng sớm dần

### Streaming Inference — Noisy-OR Online

Khi inference streaming, `p_agg` được tính **online** O(1) per turn:

```
p_agg_0 = 0
p_agg_t = 1 − (1 − p_agg_{t-1}) × (1 − q_t)    ← chỉ cần q_t mới + p_agg cũ
```

`p_agg` luôn **không giảm** → rất phù hợp cho early detection: một khi model đã "nghi ngờ" thì xác suất không thể quay lại.

## Project Structure

```
ScamStream/
├── config.py              # M1Config — hyperparameters & paths
├── model.py               # M1Classifier, CrossTurnAttention, Focal U-shape Loss,
│                          #   Weighted Prefix Auxiliary Loss (Noisy-OR)
├── dataset.py             # DialogueDataset, collate_fn, truncate_augment
├── train.py               # Training loop + streaming preview (q_t + p_agg)
├── test.py                # Evaluation & streaming report (per-turn + Noisy-OR metrics)
├── metrics.py             # compute_streaming_metrics, print_streaming_report
├── infer_stream.py        # Real-time streaming inference (Noisy-OR online)
├── analyze_errors.py      # Error analysis utilities
├── prepare_datasets.py    # Data preprocessing & conversion pipeline
└── dataset/               # Processed datasets (generated by prepare_datasets.py)
    ├── real_1.json         # Real data — half 1 (train)
    ├── real_2.json         # Real data — half 2 (test)
    ├── tele_data.json      # Tele-data (all splits merged)
    ├── synthetic_data.json # Synthetic-data (v1_v5 + v2 + v3)
    ├── real_syn.json       # Real-1 + Synthetic combined
    └── real_tele.json      # Real-1 + Tele combined
```

## Data Format

Dataset phải là file JSON với format sau:

```json
[
  {
    "label": "scam",
    "turns": [
      "A lô, chào anh ạ...",
      "Vâng, ai đầu dây đấy ạ?",
      "..."
    ]
  }
]
```

## Data Sources

| Source | Mô tả | Dialogues |
|--------|--------|-----------|
| **Real.xlsx** | Dữ liệu thực — chia 50/50 stratified → Real-1, Real-2 | 945 (486 scam, 459 harmless) |
| **Tele-data** | Dữ liệu tele (train+val+test gộp lại) | 3,600 (1,800 scam, 1,800 harmless) |
| **Synthetic-data** | Dữ liệu synthetic (v1_v5 + v2 + v3) | 700 (445 scam, 255 harmless) |

## Requirements

```bash
pip install torch transformers numpy scikit-learn openpyxl gdown
```

- **Python 3.8+**
- **PyTorch** & **Transformers**: Core models
- **numpy** & **scikit-learn**: Tính toán metrics (F1, Precision, Recall, v.v.)
- **openpyxl**: Đọc file Excel (dùng khi chạy `prepare_datasets.py`)

## Quick Start

### 0. Loading dataset

```bash
# Load dataset từ file Drive
gdown --folder 1GWNfWB_ZAAdSSI4DaWB4DjDhTqsa5vPu

```

### 1. Training

```bash
# Truyền đường dẫn dataset trực tiếp
python train.py --train-file /path/to/train.json --test-file /path/to/test.json


# Kết hợp với các flags khác
python train.py --train-file train.json --test-file test.json --debug          # 2 epochs, no aug
python train.py --train-file train.json --test-file test.json --small          # 5 epochs
python train.py --train-file train.json --test-file test.json --output-dir ./out
```

### 2. Dataset Options Summary

| `--dataset` | Train Set | Test Set | Description |
|-------------|-----------|----------|-------------|
| `real` | Real-1 (472) | Real-2 (473) | Baseline: real data only |
| `synthetic` | Synthetic (700) | Real-2 (473) | Cross-domain: synthetic → real |
| `tele` | Tele (3,600) | Real-2 (473) | Cross-domain: tele → real |
| `real_syn` | Real-1 + Syn (1,172) | Real-2 (473) | Combined: real + synthetic |
| `real_tele` | Real-1 + Tele (4,072) | Real-2 (473) | Combined: real + tele |

Output checkpoints tự động lưu vào `outputs/<dataset_option>/best_model/`.

### 3. Evaluation

```bash
python test.py --model-dir outputs/best_model
```

Output bao gồm:
- **Dialogue-level metrics**: Accuracy, F1, AUROC
- **Streaming metrics (per-turn)**: Detection rate, Avg delay, False alarm rate
- **Noisy-OR p_agg metrics**: Detection rate (p_agg), False alarm (p_agg), Avg alert turn (p_agg)
- **Early detection stats**: Median/Mean alert turn, Alert in 1st half
- **Error analysis Excel**: Từng turn hiển thị `q_t` (evidence) + `p_agg` (Noisy-OR cumulative)

### 4. Streaming Inference

```bash
python infer_stream.py --model-dir outputs/best_model
```

Output mỗi turn:
```
T01 [████░░░░░░░░░░░░░░░░] q=0.215 p_agg=0.215
T02 [████████░░░░░░░░░░░░] q=0.302 p_agg=0.452
T03 [████████████████░░░░] q=0.610 p_agg=0.786 ← ALERT
```

- `q_t`: xác suất scam per-turn (evidence)
- `p_agg`: xác suất tích lũy Noisy-OR (monotonically non-decreasing)
- Alert khi `p_agg >= alert_thresh` (default 0.80)

## Key Hyperparameters

| Parameter | Default | Mô tả |
|-----------|---------|--------|
| `model_name` | `contextboxai/halong_embedding` | Backbone encoder |
| `max_turn_len` | 96 | Max tokens per turn |
| `max_turns` | 20 | Max turns per dialogue |
| `hidden_dim` | 256 | Hidden dimension |
| `attn_heads` | 4 | Attention heads in CrossTurnAttention |
| `focal_gamma` | 2.0 | Focal loss gamma |
| `w_floor` | 0.1 | U-shape weight minimum |
| `class_weight_harmless` | 8.0 | Class weight for harmless samples |
| `weighted_lambda` | 0.5 | λ for Weighted Prefix auxiliary loss (0 = OFF) |
| `batch_size` | 2 | Batch size |
| `grad_accum_steps` | 8 | Gradient accumulation (effective batch = 16) |
| `lr` | 2e-5 | Learning rate |
| `num_epochs` | 10 | Total epochs |
| `unfreeze_epoch` | 3 | Epoch to unfreeze encoder (lr × 0.1) |
| `truncate_aug` | True | Truncated conversation augmentation |
| `alert_thresh` | 0.80 | Streaming alert threshold (p_agg) |

## Loss Ablation

Bạn có thể thử các cấu hình loss khác nhau bằng cách thay đổi `weighted_lambda` trong `config.py`:

| Cấu hình | `weighted_lambda` | Mô tả |
|----------|-------------------|-------|
| **Focal U-shape only** | `0.0` | Loss gốc, không có auxiliary |
| **Focal + Prefix (default)** | `0.5` | Kết hợp cả hai, thúc đẩy early detection |
| **Strong Prefix** | `1.0` | Đánh trọng số auxiliary bằng main loss |
